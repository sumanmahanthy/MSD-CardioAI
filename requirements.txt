# CardioAI Backend — HuggingFace Spaces Requirements
#
# NOTE: HuggingFace auto-installs these — DO NOT re-specify them:
#   gradio, uvicorn, torch, numpy, websockets
# Specifying them causes version conflict errors in the build.

# ── Web Server (only extras not auto-installed) ───────────────────────────────
fastapi>=0.115.2
python-multipart>=0.0.9

# ── Deep Learning extras (torch itself is pre-installed by HF) ────────────────
torchvision>=0.16.0
timm>=0.9.12

# ── Image Processing ──────────────────────────────────────────────────────────
opencv-python-headless>=4.9.0
Pillow>=10.0.0

# ── Explainability (Grad-CAM) ─────────────────────────────────────────────────
grad-cam>=1.4.8

# ── ML Utilities ──────────────────────────────────────────────────────────────
scikit-learn>=1.3.0
